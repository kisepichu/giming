use proconio::{input, fastout};
fn solve(A: i64, B: i64) {
  //
}
#[fastout]
fn main() {
  let A: i64;
  let B: i64;
  {
    input! { tmp: i64 };
    A = tmp;
  }
  {
    input! { tmp: i64 };
    B = tmp;
  }
  solve(A, B);
}
